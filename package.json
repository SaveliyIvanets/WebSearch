{
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test dist/logger/Logger.test.js"
  },
  "dependencies": {
    "cheerio": "^1.2.0"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "tsup": "^8.5.1",
    "typescript": "^7.0.2"
  }
}
